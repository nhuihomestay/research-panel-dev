{
    "version": 2,
    "builds": [{
        "src": "src/index.ts"
        "use": "@vercel/node"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/src"
    }],
    "rewrites": [{
        "source": "/(.*)",
        "destination": "/src"
    }]
}
