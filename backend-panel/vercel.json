{
    "version": 2,
    "builds": [{
        "src": "index.ts",
        "use": "@vercel/node"
    }],
    "rewrites": [{
        "source": "/api/(.*)",
        "destination": "/api"
    }],
    "env": {
        "NODE_ENV": "prod"
    }
}